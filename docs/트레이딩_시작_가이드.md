# 알고리즘 트레이딩 시작 가이드

> **당신의 현재 위치**: LLM Engineer, CUDA 학습 중, 밤 10시 이후 생산성 최고  
> **목표**: 2026년까지 안정적인 트레이딩 시스템 구축  
> **시간 투자**: 주 10시간 (평일 2시간 × 3일 + 주말 4시간)

---

## 🎯 Phase 0: 준비 단계 (Week 1-2)

### Week 1: 학습 & 환경 구축

#### Day 1-2: 이론 학습 (총 4시간)
```
[ ] "Algorithmic Trading" by Ernie Chan 1-3장 읽기 (2시간)
    - 무엇이 "엣지"인가?
    - 백테스트의 함정
    - 실전 vs 백테스트 차이

[ ] YouTube: "Quantitative Trading" by QuantInsti (2시간)
    - 검색: "algorithmic trading for beginners"
    - 추천: "Zipline Tutorial", "Backtrader Crash Course"

핵심 개념 정리:
- Sharpe Ratio가 뭐고 왜 중요한가?
- Look-ahead Bias란?
- Walk-forward Optimization이란?
```

#### Day 3-4: 개발 환경 구축 (총 4시간)
```bash
# Python 환경 설정
conda create -n trading python=3.10
conda activate trading

# 필수 라이브러리
pip install pandas numpy matplotlib
pip install ta-lib  # 기술적 지표
pip install backtrader  # 백테스트 엔진
pip install yfinance  # 무료 데이터 (미국 주식)

# 선택 (나중에)
pip install tensorflow pytorch  # ML 모델용

# 한국 주식 데이터
pip install pykrx  # 한국거래소 데이터
```

**테스트 코드 실행**
```python
# test.py
import pandas as pd
import yfinance as yf
import backtrader as bt

# 데이터 가져오기 테스트
data = yf.download('AAPL', start='2020-01-01', end='2023-12-31')
print(data.head())
print("환경 구축 완료!")
```

#### Day 5-6: 첫 백테스트 작성 (총 4시간)
```python
# my_first_strategy.py
import backtrader as bt

class SimpleSMA(bt.Strategy):
    """
    가장 간단한 전략: 이동평균 크로스
    - 단기 MA > 장기 MA → 매수
    - 단기 MA < 장기 MA → 매도
    """
    params = (
        ('short_period', 10),
        ('long_period', 30),
    )
    
    def __init__(self):
        self.short_ma = bt.indicators.SMA(self.data.close, period=self.params.short_period)
        self.long_ma = bt.indicators.SMA(self.data.close, period=self.params.long_period)
        self.crossover = bt.indicators.CrossOver(self.short_ma, self.long_ma)
    
    def next(self):
        if not self.position:  # 포지션 없으면
            if self.crossover > 0:  # 골든크로스
                self.buy()
        else:  # 포지션 있으면
            if self.crossover < 0:  # 데드크로스
                self.close()

# 백테스트 실행
cerebro = bt.Cerebro()
cerebro.addstrategy(SimpleSMA)

# 데이터 로드
data = bt.feeds.PandasData(dataname=yf.download('AAPL', start='2020-01-01', end='2023-12-31'))
cerebro.adddata(data)

# 초기 자본
cerebro.broker.setcash(100000.0)

# 수수료 (0.1%)
cerebro.broker.setcommission(commission=0.001)

print(f'Starting Portfolio Value: {cerebro.broker.getvalue():.2f}')
cerebro.run()
print(f'Final Portfolio Value: {cerebro.broker.getvalue():.2f}')

cerebro.plot()
```

**과제**: 
- [ ] AAPL로 실행 → 수익률 계산
- [ ] 파라미터 바꿔보기 (short: 5, 10, 20 / long: 20, 30, 50)
- [ ] 다른 종목 시도 (TSLA, MSFT)

#### Day 7: 회고 & 계획 (1시간)
```
질문:
1. 백테스트 결과가 좋았나?
2. 파라미터 바꾸면 결과가 크게 달라지나? (과최적화 신호)
3. 이 전략의 "엣지"는 뭐라고 생각하나?

다음 주 목표:
- 더 정교한 전략 1개 구현
- 백테스트 성과 측정 자동화
```

---

### Week 2: 데이터 수집 & 분석

#### Day 1-2: 한국 주식 데이터 수집 (총 4시간)
```python
# korean_data.py
from pykrx import stock
import pandas as pd

# KOSPI 200 종목 리스트
tickers = stock.get_market_ticker_list("20231201", market="KOSPI")
print(f"Total tickers: {len(tickers)}")

# 삼성전자 데이터
df = stock.get_market_ohlcv("20200101", "20231231", "005930")
print(df.head())

# 여러 종목 한번에
def get_multiple_stocks(tickers, start, end):
    data = {}
    for ticker in tickers[:10]:  # 테스트: 10개만
        try:
            df = stock.get_market_ohlcv(start, end, ticker)
            data[ticker] = df
            print(f"✓ {ticker}")
        except:
            print(f"✗ {ticker} - failed")
    return data

# 실행
data = get_multiple_stocks(tickers[:10], "20200101", "20231231")

# CSV 저장
for ticker, df in data.items():
    df.to_csv(f"data/{ticker}.csv")
    
print(f"Saved {len(data)} stocks to data/")
```

**과제**:
- [ ] KOSPI 200 전체 종목 다운로드
- [ ] 데이터 품질 체크 (결측치, 이상치)
- [ ] 디렉토리 구조 만들기
```
trading_system/
├── data/
│   ├── korean_stocks/
│   └── us_stocks/
├── strategies/
├── backtests/
└── notebooks/
```

#### Day 3-4: 탐색적 데이터 분석 (총 4시간)
```python
# analysis.py
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 삼성전자 분석
df = pd.read_csv("data/005930.csv", parse_dates=['날짜'], index_col='날짜')

# 수익률 계산
df['returns'] = df['종가'].pct_change()

# 시각화
fig, axes = plt.subplots(2, 2, figsize=(15, 10))

# 1. 가격 차트
axes[0, 0].plot(df.index, df['종가'])
axes[0, 0].set_title('삼성전자 주가')

# 2. 거래량
axes[0, 1].bar(df.index, df['거래량'])
axes[0, 1].set_title('거래량')

# 3. 일별 수익률 분포
axes[1, 0].hist(df['returns'].dropna(), bins=50)
axes[1, 0].set_title('수익률 분포')

# 4. 변동성 (20일 이동 표준편차)
df['volatility'] = df['returns'].rolling(20).std()
axes[1, 1].plot(df.index, df['volatility'])
axes[1, 1].set_title('변동성')

plt.tight_layout()
plt.savefig('analysis_samsung.png')
plt.show()

# 통계 요약
print("\n=== 삼성전자 통계 ===")
print(f"평균 일수익률: {df['returns'].mean():.4f} ({df['returns'].mean()*252:.2%} 연환산)")
print(f"변동성: {df['returns'].std():.4f} ({df['returns'].std()*252**0.5:.2%} 연환산)")
print(f"Sharpe (무위험 0 가정): {df['returns'].mean()/df['returns'].std() * 252**0.5:.2f}")
print(f"최대 일수익률: {df['returns'].max():.2%}")
print(f"최소 일수익률: {df['returns'].min():.2%}")
```

**인사이트 도출**:
- [ ] 어느 시간대에 변동성이 높은가?
- [ ] 거래량과 수익률의 상관관계는?
- [ ] 급등/급락 패턴이 있나?

#### Day 5-6: 간단한 전략 백테스트 (총 4시간)
```python
# mean_reversion_strategy.py
class BollingerBandMeanReversion(bt.Strategy):
    """
    볼린저밴드 역추세 전략
    - 하단 밴드 이탈 → 매수 (과매도)
    - 상단 밴드 이탈 → 매도 (과매수)
    """
    params = (
        ('period', 20),
        ('devfactor', 2.0),
    )
    
    def __init__(self):
        self.bbands = bt.indicators.BollingerBands(
            self.data.close,
            period=self.params.period,
            devfactor=self.params.devfactor
        )
    
    def next(self):
        if not self.position:
            # 하단 밴드 아래로 가격이 떨어지면 매수
            if self.data.close[0] < self.bbands.lines.bot[0]:
                self.buy()
        else:
            # 중간선 돌파하면 익절
            if self.data.close[0] > self.bbands.lines.mid[0]:
                self.close()
            # 상단 밴드 돌파하면 익절
            elif self.data.close[0] > self.bbands.lines.top[0]:
                self.close()

# 백테스트 실행 + 성과 측정
cerebro = bt.Cerebro()
cerebro.addstrategy(BollingerBandMeanReversion)

# 한국 데이터
df = pd.read_csv("data/005930.csv", parse_dates=['날짜'], index_col='날짜')
df.columns = ['open', 'high', 'low', 'close', 'volume']  # 영문으로 변경
data = bt.feeds.PandasData(dataname=df)
cerebro.adddata(data)

cerebro.broker.setcash(10000000)  # 1천만원
cerebro.broker.setcommission(commission=0.0003)  # 0.03% 수수료

# 분석기 추가
cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe')
cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
cerebro.addanalyzer(bt.analyzers.Returns, _name='returns')

print(f'시작 자본: {cerebro.broker.getvalue():,.0f}원')
results = cerebro.run()
print(f'최종 자본: {cerebro.broker.getvalue():,.0f}원')

# 성과 출력
strat = results[0]
print(f"\nSharpe Ratio: {strat.analyzers.sharpe.get_analysis()['sharperatio']:.2f}")
print(f"Max Drawdown: {strat.analyzers.drawdown.get_analysis()['max']['drawdown']:.2f}%")
print(f"Total Return: {strat.analyzers.returns.get_analysis()['rtot']:.2%}")
```

**과제**:
- [ ] 5개 종목에 대해 실행
- [ ] 파라미터 최적화 (period: 10, 20, 30 / devfactor: 1.5, 2.0, 2.5)
- [ ] 결과 스프레드시트에 정리

#### Day 7: Week 2 회고 (1시간)
```
체크리스트:
[ ] 한국 주식 데이터 수집 완료 (최소 50개 종목)
[ ] 백테스트 자동화 스크립트 작성
[ ] 첫 전략 성과 측정 (Sharpe, MDD, 승률)

발견한 것:
1. 어떤 전략이 가장 좋았나?
2. 모든 종목에서 잘 작동했나? (아니라면 왜?)
3. 백테스트 결과를 믿을 수 있나? (look-ahead bias 없나?)

다음 주 목표:
- 리스크 관리 추가 (stop loss, position sizing)
- 여러 전략 조합
- Walk-forward 최적화
```

---

## 🚀 Quick Start Checklist (바로 시작하고 싶다면)

### 오늘 바로 할 수 있는 것 (2시간)

```bash
# 1. 환경 설정 (15분)
conda create -n trading python=3.10
conda activate trading
pip install pandas numpy matplotlib yfinance backtrader pykrx

# 2. 첫 백테스트 실행 (45분)
# 위의 SimpleSMA 코드 복사 → 실행

# 3. 결과 분석 (30분)
# 수익률 계산, 그래프 확인

# 4. 파라미터 변경 실험 (30분)
# short_period, long_period 바꿔가며 테스트
```

**오늘의 목표**: "백테스트가 뭔지 감을 잡는다"

---

## 📊 Week 3-4 예고: 본격 전략 개발

### Week 3: 스캘핑 전략 (호가창 분석)
```python
# 실시간 데이터 필요 → KIS API 연동
# 호가 불균형 계산
# 틱 데이터 분석
```

### Week 4: 머신러닝 시도
```python
# LSTM으로 가격 방향 예측
# Feature Engineering (기술적 지표 50개)
# 예측 정확도 vs 실제 수익률
```

---

## 💡 당신에게 맞는 시간표

### 평일 (월, 수, 금) - 밤 10시~12시 (2시간)
```
22:00-22:30  코드 작성 / 백테스트 실행
22:30-23:30  결과 분석 / 문제 디버깅
23:30-24:00  다음 실험 계획
```

### 주말 (토요일) - 4시간
```
14:00-16:00  새로운 전략 아이디어 구현
16:00-18:00  여러 종목/기간으로 테스트
```

### 주말 (일요일) - 1시간
```
20:00-21:00  주간 회고 + 다음 주 계획
```

**총 주 11시간** (목표: 10시간)

---

## ❓ 자주 하는 질문

### Q1: "코딩은 할 줄 아는데 금융 지식이 없어요"
→ 괜찮습니다. 필요한 것만 배우세요.
- Sharpe Ratio (리스크 대비 수익)
- Max Drawdown (최대 낙폭)
- 볼린저밴드, RSI, 이동평균 (구글 10분이면 충분)

### Q2: "어떤 전략부터 시작해야 하나요?"
→ 순서:
1. 이동평균 크로스 (가장 간단)
2. 볼린저밴드 역추세 (평균 회귀)
3. 브레이크아웃 (모멘텀)
4. 페어 트레이딩 (통계적 차익거래)
5. 머신러닝 (LSTM, RL) ← 나중에

### Q3: "실제 돈으로 언제 시작하나요?"
→ 최소 3개월 후
- 1개월: 학습 + 백테스트
- 1개월: Paper trading (모의투자)
- 1개월: 소액 실전 (500만원)

그리고 나서 Sharpe > 1.5 이상이면 확대.

### Q4: "수수료/슬리피지는 얼마나 설정하나요?"
→ 한국장 기준:
```python
COMMISSION = 0.0003  # 0.03% (증권사마다 다름)
TAX = 0.0023  # 0.23% (매도만)
SLIPPAGE = 0.001  # 0.1% (유동성 있는 종목)
# 초단타일수록 더 크게 (0.2-0.5%)
```

### Q5: "혼자 하기 힘들어요"
→ 커뮤니티 활용:
- Reddit: r/algotrading
- Discord: "Algorithmic Trading Korea" 검색
- GitHub: 다른 사람 코드 보기

---

## 🎁 보너스: 즉시 사용 가능한 템플릿

### 백테스트 자동화 스크립트
```python
# auto_backtest.py
import backtrader as bt
import pandas as pd
from glob import glob

class AutoBacktester:
    def __init__(self, strategy, data_folder='data/'):
        self.strategy = strategy
        self.data_folder = data_folder
        self.results = []
    
    def run(self):
        files = glob(f"{self.data_folder}/*.csv")
        
        for file in files:
            ticker = file.split('/')[-1].replace('.csv', '')
            
            cerebro = bt.Cerebro()
            cerebro.addstrategy(self.strategy)
            
            df = pd.read_csv(file, parse_dates=[0], index_col=0)
            data = bt.feeds.PandasData(dataname=df)
            cerebro.adddata(data)
            
            cerebro.broker.setcash(10000000)
            cerebro.broker.setcommission(commission=0.0003)
            
            cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe')
            cerebro.addanalyzer(bt.analyzers.DrawDown, _name='dd')
            
            start_value = cerebro.broker.getvalue()
            result = cerebro.run()[0]
            end_value = cerebro.broker.getvalue()
            
            self.results.append({
                'ticker': ticker,
                'return': (end_value - start_value) / start_value,
                'sharpe': result.analyzers.sharpe.get_analysis().get('sharperatio', 0),
                'max_dd': result.analyzers.dd.get_analysis()['max']['drawdown']
            })
            
            print(f"✓ {ticker}: Return={self.results[-1]['return']:.2%}, Sharpe={self.results[-1]['sharpe']:.2f}")
        
        # 결과를 DataFrame으로
        return pd.DataFrame(self.results).sort_values('sharpe', ascending=False)

# 사용법
backtester = AutoBacktester(SimpleSMA)
results = backtester.run()
results.to_csv('backtest_results.csv')
print("\nTop 5 Performers:")
print(results.head())
```

---

## 🚨 시작 전 마지막 체크

```
[ ] 이 프로젝트에 최소 6개월 투자할 각오가 되어 있나?
[ ] 초기 손실을 감당할 수 있나? (첫 3개월 손실 가능성 70%)
[ ] 감정적으로 거래하지 않을 자신 있나?
[ ] 이미 만들어진 "성배"를 찾는게 아니라 "시스템"을 만드는거라는걸 이해했나?
[ ] 일확천금이 아니라 안정적인 연 20-40% 목표라는걸 동의하나?
```

모두 체크했다면 → **지금 바로 시작하세요!**

---

**작성일**: 2025-12-20  
**대상**: 트레이딩 시스템 구축 시작하는 개발자  
**예상 소요**: Phase 0 완료까지 2주

